services:
  relationship-journal:
    build: .
    container_name: relationship-journal
    restart: unless-stopped
    ports:
      - "8080:8080"
    volumes:
      - ./data:/app/data
    env_file:
      - .env.local
    environment:
      - TZ=Asia/Shanghai
      - DATA_DIR=/app/data
